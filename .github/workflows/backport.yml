name: Backport
on:
  pull_request:
    types:
      - closed
      - labeled

jobs:
  backport:
    runs-on: ubuntu-18.04
    name: Backport Action
    steps:
      - name: Backport
        uses: sqren/backport-github-action@1.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
